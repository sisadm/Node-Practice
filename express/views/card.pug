extends layout.pug

block content
  section#content
    h2= text
    if hint
      p
        i Hint: #{hint}
    
    if hint 
      a(href=`${link}`) Answer
    else 
      a(href=`${link}`) Question 